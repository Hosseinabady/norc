package com.hosseinabady.norc_designer_plugin.widget.pattern;

import java.util.ArrayList;

import org.eclipse.draw2d.ColorConstants;
import org.eclipse.swt.SWT;
import org.eclipse.swt.widgets.Canvas;
import org.eclipse.swt.widgets.Composite;
import org.eclipse.swt.widgets.Label;


public class MappedTaskPatternCanvas extends Canvas {

	
	public MappedTaskPatternCanvas(Composite parent, int style) {
		super(parent, style);
		
		
/*		Composite composite1 = new Composite(parent, SWT.NONE);
		

		ArrayList<Label> leftCore = new ArrayList<Label>();
		ArrayList<Label> rightCore = new ArrayList<Label>();
		for (int i = 0; i < 10; i++)
			for (int j = 0; j < 10; j++) {
				leftCore.add(new Label(composite1, SWT.NONE));
				rightCore.add(new Label(composite1, SWT.NONE));
				int index = j*i+i;
				leftCore.get(index).setText("t1");
				leftCore.get(index).setBackground(ColorConstants.gray);
				leftCore.get(index).setSize(10,20);
				leftCore.get(index).setLocation(10*index,0);
				
				rightCore.get(index).setText("t1");
				rightCore.get(index).setBackground(ColorConstants.gray);
				rightCore.get(index).setSize(10,20);
				rightCore.get(index).setLocation(-10*index,0);
			}
		leftCore.add(new Label(composite1, SWT.NONE));
		
		int index = 0;
		leftCore.get(index).setText("t1");
		leftCore.get(index).setBackground(ColorConstants.gray);
		leftCore.get(index).setSize(20,20);
		leftCore.get(index).setLocation(10*index,0);
*/		 
	
	}
}
